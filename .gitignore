node_modules/
dist/
.env
.env.local
.next/
.turbo/
*.tsbuildinfo

# Dolt database files (added by bd init)
.dolt/
*.db
resume-outputs/

# Session memory (local only — see CLAUDE.md session memory section)
memory/

# Audit / phase retrospective docs (transient, not repo history)
AUDIT.md
*-AUDIT.md

# macOS cruft
.DS_Store
**/.DS_Store
